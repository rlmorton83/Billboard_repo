<%= form_with(model: billboard, local: true) do |form| %>
  <% if billboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(billboard.errors.count, "error") %> prohibited this billboard from being saved:</h2>

      <ul>
      <% billboard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
